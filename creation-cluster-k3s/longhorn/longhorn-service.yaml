apiVersion: v1
kind: Service
metadata:
  labels:
    app: longhorn-ui
  name: longhorn-frontend
  namespace: longhorn-system
spec:
  ports:
  - name: web
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: longhorn-ui
  type: LoadBalancer
